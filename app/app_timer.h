#ifndef APP_TIMER_H__
#define APP_TIMER_H__

#include "timer.h"

extern DDA_Err_t Timer3Init(void);
extern DDA_Err_t Timer2Init(void);

extern DDA_Err_t TimerInit(void);

extern DDA_Err_t Timer5Init(void);
extern DDA_Err_t Timer6Init(void);

#endif
